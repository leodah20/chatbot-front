# ============================================
# Arquivo .env - TEMPLATE
# ============================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env na raiz do projeto
# 2. Preencha TODOS os valores abaixo (são OBRIGATÓRIOS)
# 3. NUNCA commite o arquivo .env no Git (já está no .gitignore)
#
# Comando para copiar:
# Windows: copy .env.example .env
# Linux/Mac: cp .env.example .env
# ============================================

# ============================================
# ⚠️ VARIÁVEIS OBRIGATÓRIAS
# ============================================

# Chave secreta para criptografia de sessões Flask
# OBRIGATÓRIO: Gere uma chave segura usando o comando abaixo:
# python -c "import secrets; print(secrets.token_hex(32))"
# 
# IMPORTANTE: Use uma chave diferente para desenvolvimento e produção!
# NUNCA compartilhe esta chave publicamente!
SECRET_KEY=cole-aqui-a-chave-gerada-pelo-comando-acima

# URL base da API Backend (ChatBot_API)
# OBRIGATÓRIO: URL onde a API backend está rodando
# Se a API estiver rodando localmente na porta 8000:
API_BASE_URL=http://127.0.0.1:8000

# ============================================
# Configurações Recomendadas
# ============================================

# Nome do arquivo principal da aplicação Flask
FLASK_APP=app.py

# Ambiente: 'development' (desenvolvimento) ou 'production' (produção)
FLASK_ENV=development

# Modo debug (True/False)
# Em produção, sempre deixe como False
DEBUG=True

# ============================================
# Configurações Opcionais
# ============================================

# Porta onde a aplicação Flask irá rodar (padrão: 5000)
# FLASK_RUN_PORT=5000

# Host onde a aplicação Flask irá rodar (padrão: 127.0.0.1)
# Para permitir acesso externo, use: 0.0.0.0
# FLASK_RUN_HOST=127.0.0.1

# Timeout para requisições à API (em segundos, padrão: 10)
# API_TIMEOUT=10

# ============================================
# Notas Importantes
# ============================================
#
# 1. SECRET_KEY: Esta chave é OBRIGATÓRIA e usada para criptografar
#    dados de sessão. Sem ela, a aplicação NÃO irá iniciar.
#
# 2. API_BASE_URL: Esta URL é OBRIGATÓRIA. Certifique-se de que a
#    API backend está rodando e acessível nesta URL.
#
# 3. O arquivo .env é OBRIGATÓRIO - a aplicação não funcionará sem ele.
#
# 4. Consulte o README.md para instruções detalhadas sobre como
#    gerar o SECRET_KEY e configurar o arquivo .env.
#
# ============================================
